# React example

Generate the complete optional React/Vite starter from this repository root:

```bash
pyrora new realtime-app --react
cd realtime-app
pyrora frontend install
pyrora frontend dev
```

The generated starter contains API, WebSocket, and native WebRTC signaling helpers.
